/**
 * Created by i.sungurov on 02.10.13.
 */

var operatorShell = angular.module('operatorShell', ['ngCookies', 'LocalStorageModule'])
    .run(['authentication', function (authentication) {
        'use strict';
    }]);
